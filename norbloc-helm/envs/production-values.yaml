# Production overrides — larger resources, real hostname
ingress:
  host: "norbloc.com"

mariadb:
  persistence:
    size: 10Gi
  auth:
    # TODO: Replace with real production credentials (or use Vault)
    rootPassword: "cGFzc3dvcmQ="
    userPassword: "cGFzc3dvcmQ="

wordpress:
  debug: "0"
  persistence:
    size: 10Gi
