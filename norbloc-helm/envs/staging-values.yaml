# Staging overrides — smaller resources, local hostname
ingress:
  host: "staging.localhost"

mariadb:
  persistence:
    size: 2Gi
  auth:
    # Staging-specific passwords (base64). Rotate before going live.
    rootPassword: "c3RhZ2luZy1yb290"
    userPassword: "c3RhZ2luZy11c2Vy"

wordpress:
  debug: "1"
  persistence:
    size: 2Gi
  image:
    tag: "6.9.4-apache"


mariadb:
  image:
    tag: "11.8"
